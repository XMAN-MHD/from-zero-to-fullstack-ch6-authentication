{
  "name": "from-zero-to-fullstack-ch6-authentication",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:frontend": "npm --prefix frontend run lint"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7"
  }
}